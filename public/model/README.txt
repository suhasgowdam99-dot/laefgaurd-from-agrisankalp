# CUSTOM MODEL WORKSPACE

Place your trained 20-class model files here:
1. model.json
2. group1-shard1of1.bin (and any other .bin shards)

The website is configured to load these files automatically from this location.
